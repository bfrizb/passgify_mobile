import React from "react";
import { Text, View } from "react-native";

// TODO (P3): Group Styles together rather than repeating them | Look into React Native Paper THEMES
// TODO (P5): Convert to TypeScript

// TODO (P4): Text Below
export default function SettingsScreen() {
  return (
    <View style={{ padding: "5%" }}>
      <Text style={{ fontSize: 40, marginVertical: 10, color: "gray" }}>
        About
      </Text>
      <Text>TODO</Text>
    </View>
  );
}
